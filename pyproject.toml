[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "ai-job-search-agent"
version = "1.0.0"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
# Automatically discovers: agents/, config/, utils/
where = ["."]
